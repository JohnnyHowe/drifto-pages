import React from 'react';

export default function ScoresResetExplanation() {
    return <div>
        <h1>Scores Reset (May 2023)</h1>
        <p>Short answer: The scoring and difficulty have changed so much that old high scores are completely unobtainable.
This renders the leaderboard pretty much useless. So all scores have been reset to make the top scores obtainable again.
        </p>
    </div>
}